# Simple-plan example

This directory demonstrates one self-contained milestone plan with embedded
tasks, inline inputs/evidence/receipts, and a repository-local adapter. Copy
`PLAN.md` and the `.acdd-legacy/` adapter/contract bundle, then replace the example
content and declared paths without changing the canonical
`acdd/plan/simple/v1` shape.
